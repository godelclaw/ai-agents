# MeTTapedia Papers & Literature

## Directory Structure

### `./` — MeTTapedia project papers (our work)
| File | Topic |
|------|-------|
| `wm-pln-book_v3.tex` / `_v3_oruzi.tex` | World Model Calculus book (main deliverable) |
| `nuPLN_v1.pdf` | nuPLN: next-generation PLN |
| `xiPLN.pdf` | ξPLN: PLN with imprecise probabilities |
| `PLN-kNN-NB.pdf` | PLN ↔ kNN / Naive Bayes bridge |
| `leanOSLF.pdf` | OSLF Lean formalization |
| `MeTTaIL.pdf` | MeTTa Intermediate Language |
| `modal-bridges.pdf` | Modal logic bridges |
| `mq-calculus-lean-formalization.pdf` | MQ-calculus Lean formalization |
| `pnp_crux.pdf` | Crux-first Lean notes on Goertzel's P vs NP proof attempt: locality, fixed-width VV isolation, the pairwise-survivor and candidate-wise moment bridges, the pairwise-columns hashing obstruction, RHS-bias irrelevance under uniform labels and under exact finite 2-universality, the conditioning obstruction for post-uniqueness label collapse, compression, symmetrization, exact post-switch-input inconsistency, exact post-switch fork, orbit-neutrality, fiber-neutrality, weighted fiber-neutrality, invariant-score, weight-asymmetry, side-channel-resolution, resolution-demand, residual-symmetry, asymmetry-budget, short-program, metagraph/interface, and tree-message obstructions |
| `pnp_grassroots.pdf` | Ground-up Lean background theory for the Goertzel program: encoded hypothesis classes, finite ERM and weighted recovery bounds, the same-route certificate, and the current Meredith/GSLT limits that weakness summaries are global, the exact HML layer is infinitary by default, and the exact rho present-moment surface already realizes powerset-like finite probe slices |
| `ns_crux.pdf` | Lean-backed NS interface note: cutoff continuity, a concrete weak-topology countermodel, an infinite-mode product-topology `ℓ¹` countermodel, a concrete chart/truncation approximation model, a genuine infinite-mode geometric truncation model, and on that same mode state an abstract good observable class, an abstract tail-spike no-go theorem, a continuity theorem for summable geometric observables, a product-topology obstruction for stronger tail detectors, a first soft-vs-hard chart fork, a more manuscript-shaped Sobolev-style soft-vs-hard chart fork, an energy-style soft-vs-hard chart fork, a log-energy soft-vs-hard chart fork, and finally a generic energy-profile fork theorem with identity, clipped-log, bounded rational saturation, smooth exponential saturation, smooth-transition instances, a positive-parameter smooth-transition family, an affine two-parameter smooth-transition family, and a proven windowed-difference profile that leaves the `EnergyProfile` shell, together with a generic hard-detector collapse theorem, plus topological Cole–Hopf transport, approximation interface, finite identity-energy/vorticity algebra, the kernel-semigroup wrapper, a theorem-statement-back Fefferman target interface isolating the current uniform-vorticity tendril, a constructive top-down bridge interface decomposing the missing lift into candidate fields, regularity, dynamics, energy, and vorticity-compatibility layers, a concrete `ℝ × ℝ^3` Navier–Stokes equation surface with explicit momentum equation, incompressibility, and initial-condition clauses together with the honest scalar-component shadow seen by the current bridge, and now a partially concrete PDE kit where divergence and divergence-free initial data are the actual trace of the spatial Fréchet derivative on the standard basis of `ℝ^3`, a more concrete constructor fixes pressure gradient to the actual spatial gradient on `ℝ^3`, a still more concrete constructor fixes the time-derivative slot to the actual time Fréchet derivative in the unit time direction, and the current most concrete constructor also fixes convection to the spatial Fréchet derivative applied to the current velocity vector, a compatibility-frontier interface isolating the last remaining theorem once those other layers are fixed, four positive self-model realizations showing the current tendril already closes bounded, seeded, bounded-seeded, and pressure-seeded versions of the target, four explicit non-self frontiers, scalar-rescaled, affine seed-blended, nonlinear saturated, and frozen-seed-gated, a generic seed/live profile shell that subsumes those frontiers, a first non-pointwise seed/live operator shell with an anchored blend instance, a finite sampled-kernel shell sitting between the pointwise and fully operator-level frontiers, a translation-kernel shell refining those sampled kernels into a first finite convolution-style family, a concrete windowed sampled-kernel bridge showing the actual affine seed-blend route is exactly a one-sample kernel instance, a concrete windowed shift-kernel bridge showing the first spatial anchored blend route is a finite translation-kernel instance, a concrete shift-kernel obstruction giving witness-forced laws `a=1`, `b=0`, and `d=0` at that first spatial same-route mouth, a finite masked-shift-kernel obstruction showing the same spatial rigidity already governs a nontrivial finite family through total coefficient sums, a general finite shift-kernel obstruction showing that at the full linear translation-kernel shell the total zero-anchor seed-plus-live mass is forced to `1` under the corresponding nonzero-initial witness pattern while the zero-shift live mass is forced to `1` and anchorwise seeded or live masses are forced to `0` under the other witness patterns, with matching bridge-level impossibility corollaries, plus Lean specialization theorems showing that the older masked common-shift shell and the older identity-anchor sampled-kernel sum law are both recovered from that newer full shift-kernel theorem, with the nondegenerate `s ≠ 0` caveat kept explicit on the masked side, and saturated sampled-kernel, masked-shift-kernel, and general shift-kernel obstructions showing the nonlinear saturated route already leaves the diagonal one-sample shell, the widened finite spatial shell, and then the full finite translation-kernel shell on unequal nonzero witness magnitudes, while the same full nonlinear shell now also exposes the zero-anchor seed-plus-live total-mass law needed for identity-anchor specialization, a matching specialization theorem that recovers the older identity-anchor sampled-kernel saturated rigidity directly from the top nonlinear shell, a constructive constant-magnitude frontier showing that in the degenerate regime `|ω(t,x)| = r` the saturated descendant is exactly a zero-shift live-only finite shift kernel with coefficient `a / (1 + r)`, then a widened invariant finite-kernel corridor where stationarity, shift-invariance, and the total-mass law `a_{\mathrm{seed}} + a_{\mathrm{live}} = a / (1 + r)` suffice to realize the saturated descendant, then a full-shell exactness theorem saying that under the same invariance assumptions and nonzero total coefficient mass any exact equality with the saturated descendant forces all nonzero witness magnitudes to agree, with the matching proof-carrying bridge-level `¬ ∃ B` corollary in the same shell, then a concrete anchored spatial specialization showing that the first windowed spatial blend route also survives inside that invariant corridor, then an anchored exactness theorem saying that if this concrete spatial candidate equals the saturated descendant with nonzero total coefficient sum, then all nonzero witness magnitudes must agree, and finally a specialization theorem showing that this anchored exactness result is itself recovered from the newer full nonlinear invariant-shell theorem; manuscript-specific NS claims are intentionally omitted until formalized in Lean |
| `native-grammatical-formalism.pdf` | Native grammatical formalism |
| `lp-core-petta.pdf` | LP core PeTTa |
| `ks-math-foundations.pdf` | Knuth-Skilling mathematical foundations |
| `ks-lean-overview.pdf` | Knuth-Skilling Lean overview |
| `ks-foi-review.pdf` | Knuth-Skilling / Foundations of Inference review |
| `governance-deontic-logic.pdf` | Governance via deontic logic |
| `doctext-gf-readme-v1.pdf` | DocText / GF readme |

### `meredith/` — Greg Meredith's framework papers
| File | Topic |
|------|-------|
| `hypercube-type-systems-2026-03.pdf` | **Generating Hypercubes of Type Systems** (Stay, Meredith, Wells). Modal type formers from operational semantics. Lambda theories → hypercube of typed calculi via sort slots and equational center. |
| `computation-causality-consciousness-2026-03-15.pdf` | **Computation, Causality, and Consciousness** (Meredith). Four-part work: (I) Physics from GSLTs — weight maps, Lagrangian, Hamiltonian, conservation; (II) Emergent ontology in massive agent populations — logical topology, coherence clusters; (III) Sentience & consciousness via hypercomputational fibration; (IV) Origins of life — assembly theory, replication fixed points. |

### `generated/` — Auto-generated outputs

### `hammer/` — Hammer / ATP related

## See Also

- `../../AIrxiv/` — Published/archived preprints
- `../../literature/` — External reference literature (hyperseed, 4CP, etc.)
- `../../lean-projects/ramsey36/related_literature/` — Ramsey theory references
- `../../lean-projects/fourcolor/FourColor/Goertzel_4CP/` — Four color proof papers
